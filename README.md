# Task Tracker API

A minimal FastAPI backend for a Task Tracker learning project.

## Overview

This is the initial skeleton of the Task Tracker REST API. It currently
exposes only a health-check endpoint. CRUD operations, status-transition
rules, and JSON-file persistence will be added in subsequent tasks,
following ADR-001 (in-memory storage with JSON file persistence).

## Project Structure