# Security Review

## AI Findings

| ID | Severity | File / location | Finding | Evidence | Grade | Reason | Suggested next step | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TT-SEC-001 | Medium | app/models.py:24, app/models.py:27, app/models.py:61, app/models.py:64; app/main.py:70; app/storage.py:7, app/storage.py:67, app/storage.py:75-83 | Unbounded task text fields and in-memory storage can be abused for memory/CPU exhaustion if the API is exposed. | `description`, `assignee`, and `search` are plain optional strings with no visible maximum length; tasks are held in the process-local `_tasks` dictionary and filtering scans the in-memory list. | Valid | Real production-relevant limitation because user-controlled strings and task/search workload are not fully bounded against an in-memory store. | Add maximum lengths for `description`, `assignee`, and `search`; consider pagination or task-count limits before exposure. | High |
| TT-SEC-002 | Medium | app/models.py:61, app/models.py:108; app/storage.py:75-83, app/storage.py:119-142 | PATCH can store `description=null`, creating a type mismatch and a reachable later 500 during search. | `TaskUpdate.description` accepts explicit `null`, `TaskResponse.description` expects `str`, and `model_copy` writes explicit nulls without re-validation; TestClient reproduction returned 200 for `description:null` and 500 for a later search term that misses the title. | Valid | Real reachable robustness issue because `description=null` can be stored and later cause a 500 during search. | Reject `description=null` or normalize it to `""`; add a regression test for search after null description PATCH. | High |
| TT-SEC-003 | Info | app/main.py:66, app/main.py:125, app/main.py:156, app/main.py:203, app/main.py:257; README.md:212-220 | No authentication or authorization is implemented; this is intentional for course scope but would matter outside the local learning context. | CRUD route handlers are exposed without visible auth dependencies, and the README documents that every endpoint is open and the app is not deployment-ready. | Valid | Intentional course-scope limitation, but still a real risk if the API is exposed outside the local learning context. | Keep the limitation documented for course use; add authentication and authorization before any shared or deployed environment. | High |
| TT-SEC-004 | Low | app/main.py:26-37; frontend/index.html:568 | CORS is development-oriented and allows `"null"` origin. | CORS allows localhost origins plus `"null"` and uses wildcard methods and headers; the frontend is hard-coded to `http://localhost:8000`. | Valid | Real low-severity development configuration risk; `"null"` origin and broad methods/headers should be reconsidered before deployment. | Replace development CORS with an environment-specific allowlist before deployment and remove `"null"` unless explicitly required. | High |
| TT-SEC-005 | Low | requirements.txt:1-6; Dockerfile:6, Dockerfile:21; .github/workflows/ci.yml:20-26 | Supply-chain checks are minimal: CI has no visible dependency/container scanning and Docker uses mutable base-image tags. | Dependencies are pinned in `requirements.txt`, but CI only installs dependencies and runs pytest; both Docker stages use the mutable `python:3.11-slim` tag. | Valid | Real low-severity governance/deployment hardening gap because no dependency/container scanning is visible and Docker uses mutable base-image tags. | Add dependency and container scanning to CI, and consider digest-pinning or scheduled image rebuilds. | High |

## My Manual Findings

| ID | Severity | File / location | Finding | Evidence | Grade | Reason | Suggested next step |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M-01 | Medium | app/models.py:24, app/models.py:27, app/models.py:61, app/models.py:64; app/main.py:70; app/storage.py:7, app/storage.py:67, app/storage.py:75-83 | `description`, `assignee`, and `search` inputs have no maximum length, and the app uses an in-memory task store. | The cited model and query parameters have no visible length constraints, and storage keeps all tasks in `_tasks` before list filtering. | Valid | User-controlled text and search workload can grow without visible bounds in process memory. | Add maximum lengths for `description`, `assignee`, and `search`; document and test the limits. |
| M-02 | Medium | app/models.py:61, app/models.py:108; app/storage.py:75-83, app/storage.py:119-142 | `PATCH` with `description=null` can store `None`; a later search can reach `description.casefold()` and return 500. | TestClient reproduction returned 200 for `"description": null`; a later search term that did not match the title reached `description.casefold()` and returned 500. | Valid | Explicit null is accepted by the update model and written without response-model re-validation. | Reject `description=null` or normalize it to `""`; add a TestClient regression test. |
| M-03 | Medium | app/models.py:62; app/main.py:245-249; app/storage.py:119-142; app/business_rules.py:31-35 | `PATCH` with `status=null` can bypass transition validation, store an invalid `None` status, and later cause a 500 when business-rule code expects a `TaskStatus`. | TestClient reproduction returned 200 with `"status":null`; a later status transition returned 500 because `validate_status_transition` formats `current.value`. | Valid | Explicit null skips the route's transition check and is written through by `model_copy`. | Reject explicit `null` for `status`; add regression tests for invalid PATCH status values. |
| M-04 | Low | app/models.py:63, app/models.py:110; app/storage.py:119-142 | `PATCH` with `priority=null` can store an invalid `None` priority even though tasks are expected to have `Low`, `Medium`, or `High`. | TestClient reproduction returned 200 with `"priority":null`; `TaskResponse.priority` is declared as `TaskPriority`. | Valid | Explicit null is accepted by the update model and persisted without re-validation. | Reject explicit `null` for `priority` and add a regression test. |
| M-05 | Low | app/main.py:26-37; frontend/index.html:568 | CORS allows the `"null"` origin and is configured for local development. | CORS includes localhost values plus `"null"` with wildcard methods and headers; the frontend API base is localhost. | Valid | This is acceptable for local coursework but too broad for deployment. | Use an environment-specific CORS allowlist and remove `"null"` before deployment. |
| M-06 | Info | app/main.py:66, app/main.py:125, app/main.py:156, app/main.py:203, app/main.py:257; README.md:216 | Authentication/authorization is intentionally absent for the course project, but all CRUD endpoints would be open outside that context. | README explicitly states there is no authentication, accounts, or authorization and every endpoint is open. | Valid | Intentional course-scope limitation becomes a real exposure risk if hosted or shared. | Keep documented for local use; add auth before non-local exposure. |
| M-07 | Low | requirements.txt:1-6; Dockerfile:6, Dockerfile:21; .github/workflows/ci.yml:20-26 | Dependency/CI/Docker hardening is limited: no visible dependency/container scanning and Docker uses mutable base-image tags. | CI only installs dependencies and runs pytest; Docker uses `python:3.11-slim` in both stages. | Valid | The repo lacks visible supply-chain checks beyond pinned Python package versions. | Add dependency and container scanning; consider digest-pinning Docker base images. |

## Reconciliation

| Agreement | AI-only | You-only |
| --- | --- | --- |
| TT-SEC-001 / M-01: Unbounded text/search inputs plus in-memory storage. | None. | M-03: `PATCH status=null` can bypass transition validation, corrupt task state, and later cause a 500. |
| TT-SEC-002 / M-02: `PATCH description=null` can store `None` and later produce a search 500. |  | M-04: `PATCH priority=null` can store an invalid `None` priority. |
| TT-SEC-003 / M-06: No authentication/authorization; intentional course scope but unsafe outside it. |  |  |
| TT-SEC-004 / M-05: Development-oriented CORS allowing `"null"` origin. |  |  |
| TT-SEC-005 / M-07: Minimal dependency/CI/Docker supply-chain hardening. |  |  |

AI coverage was strong on broad, externally visible security themes: resource exhaustion, CORS, auth scope, and supply-chain hardening.
The manual scan added the most value in precise API state-corruption cases, especially sibling `null` PATCH bugs beyond `description`.

## Top-3 Unified Backlog

| Rank | Finding | Severity | Owner | Next Step |
| --- | --- | --- | --- | --- |
| 1 | `PATCH status=null` can store invalid status and later cause 500s | Medium | Backend | Reject explicit `null` for `status`; add regression tests for invalid PATCH status values. |
| 2 | `PATCH description=null` can store `None` and later cause search 500s | Medium | Backend | Reject `description=null` or normalize it to `""`; add a TestClient regression test. |
| 3 | Unbounded text/search inputs with in-memory storage | Medium | Backend | Add maximum lengths for `description`, `assignee`, and `search`; document and test the limits. |
